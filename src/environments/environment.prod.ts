export const environment = {
  production: true,
  //the same from dev en except here is prod
  hostURL:'http://prod.site',
  username:'userProd1',
  token:'abcProd',
  port:8080,
  envName:'prod',
};
